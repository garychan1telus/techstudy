# Set to the Postgres user you want to connect to; 'postgres' is the default user.
# spring.datasource.username=postgres
# spring.datasource.password=cpms-6859
# spring.cloud.gcp.sql.database-name=cpmsdb-it02
# # This value is formatted in the form: [gcp-project-id]:[region]:[instance-name]
# spring.cloud.gcp.sql.instance-connection-name=jdbc:postgresql://localhost:5432/cpmsdb-it02
# # So app starts despite "table already exists" errors.
# spring.datasource.continue-on-error=true
# # Enforces database initialization
# spring.datasource.initialization-mode=always
#spring.cloud.gcp.project-id=
#spring.cloud.gcp.credentials.location=file:/path/to/service-account.json


# JPA
database=postgres
spring.datasource.url=jdbc:postgresql://localhost:5432/cpmsdb-it02
spring.datasource.username=postgres
spring.datasource.password=cpms-6859
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always

# spring.cloud.gcp.sql.instance-connection-name=jdbc:postgresql://localhost:5432/cpmsdb-it02
# spring.cloud.gcp.sql.database-name=postgres
